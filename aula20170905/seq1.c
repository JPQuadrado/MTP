#include <stdio.h>

int main ()
{	
	float L, X;
	printf("Digite o valor da lateral do quadrado \n");
	scanf("%f", &L); getchar(); // Escrita (?)
	X = L*L;
	printf("A area do quadrado e %f \n", X);
	return 0;
}
